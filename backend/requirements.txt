fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
sse-starlette==2.1.3

# Google Generative AI (Gemini)
google-genai==1.16.0

# LangGraph
langgraph==0.2.50
langchain-core==0.3.20

# Vector Store
qdrant-client==1.12.0

# Embeddings & Reranking
# torch CPU-only installed via Dockerfile to avoid bloated GPU wheels
sentence-transformers==3.2.1

# BM25
rank-bm25==0.2.2

# Document processing
pypdf2==3.0.1
python-docx==1.1.2

# Web search fallback
duckduckgo-search==6.3.7

# Evaluation
numpy==2.1.3
scikit-learn==1.5.2

# RAGAS quantitative evaluation (optional — only needed for `python -m evaluate.run_all --suite ragas`)
# Uncomment to enable. Heavy deps; functional + agentic suites run without them.
# ragas==0.2.10
# langchain-google-genai==2.0.7
# datasets==3.2.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
tenacity==9.0.0
httpx>=0.28.1
aiofiles==24.1.0
